apiVersion: v1
kind: Service
metadata:
  name: {{ include "citus-cluster.fullname" . }}
  labels:
    {{- include "citus-cluster.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "citus-cluster.selectorLabels" . | nindent 4 }}
